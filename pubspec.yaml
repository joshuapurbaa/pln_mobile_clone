name: pln_mobile_clone
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  style_resources:
    path: packages/style_resources
  onboarding: 
    path: packages/features/onboarding
  signin: 
    path: packages/features/signin
  
  cupertino_icons: ^1.0.2
  get_it: ^7.2.0
  shared_preferences: ^2.0.15
  flutter_screenutil: ^5.6.0
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^2.0.0

flutter:  
  uses-material-design: true