name: profile
publish_to: none

environment:
  sdk: '>=2.18.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  pln_mobile_clone:
    path: ../../../../pln_mobile_clone
  style_resources:
    path: ../../style_resources
  components_library:
    path: ../../components_library

  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  assets:
    - assets/